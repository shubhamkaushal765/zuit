[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Development task runner for the zuit workspace (ci, lint, fmt, bench, doc)."

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
toml = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
